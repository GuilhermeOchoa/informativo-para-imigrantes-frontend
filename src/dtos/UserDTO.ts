export type UserDTO = {
    showOnboarding: boolean,
    language: string,
	isLoggedIn: boolean, //esta logado
	justInformation: boolean, //so informacao
    name: string,
    email: string,
    type: string,
}